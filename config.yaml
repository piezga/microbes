# config.yaml
general:
  debug: false
  desktop: false  # Set to true if on desktop
  
parameters:
  datasets: ['root']
  N_top: 0  # top species to drop
  N_bottom: 0  # least abundant species
  t: 0.9  # threshold
  avoidance: False # True: left tail of p-value
  
